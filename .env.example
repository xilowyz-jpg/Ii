# OANDA practice (demo) account -- https://www.oanda.com/demo-account/tpa/personal_token
# Used only by `fxagents paper --source oanda` and `--broker oanda`.
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
OANDA_ENV=practice   # practice | live   (live is refused unless FXAGENTS_ALLOW_LIVE=1)
